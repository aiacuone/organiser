// import { getTitles } from '$lib';

// export const GET = async ({ params }) => {
// 	const { space } = params;
// 	const references = await getTitles(space);

// 	return new Response(JSON.stringify(references), { status: 200 });
// };
